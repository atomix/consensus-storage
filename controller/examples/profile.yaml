apiVersion: storage.atomix.io/v3beta2
kind: Profile
metadata:
  name: example-profile
spec:
  bindings:
    - store:
        name: example-multi-raft-store
      selector:
        foo: bar
        bar: baz
      services:
        - name: atomix.runtime.map.v1.Map
          config:
            cache:
              enabled: true
